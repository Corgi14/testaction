/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/testaction",
    assetPrefix: "/testaction/",
    output: "export",
    reactStrictMode: true,
};

export default nextConfig;
